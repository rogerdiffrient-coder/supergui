name: Verify SuperGUI 6.0.4
on:
  push:
    branches: [fix/v604-uniform-fullscreen]
permissions:
  contents: write
jobs:
  build:
    if: ${{ !contains(github.event.head_commit.message, '[v604-build]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/v604-uniform-fullscreen
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm run build
      - run: npm test
      - run: npm run check
      - name: Commit bundle
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dist/supergui.js
          git commit -m "build: generate SuperGUI 6.0.4 [v604-build]" || exit 0
          git pull --rebase origin fix/v604-uniform-fullscreen
          git push origin fix/v604-uniform-fullscreen
