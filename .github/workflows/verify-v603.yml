name: Verify SuperGUI 6.0.3
on:
  push:
    branches: [fix/fullscreen-scaling-option]
permissions:
  contents: write
jobs:
  build:
    if: ${{ !contains(github.event.head_commit.message, '[v603-build]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/fullscreen-scaling-option
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm run build
      - run: npm test
      - run: npm run check
      - name: Commit bundle
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dist/supergui.js
          git commit -m "build: generate SuperGUI 6.0.3 [v603-build]" || exit 0
          git pull --rebase origin fix/fullscreen-scaling-option
          git push origin fix/fullscreen-scaling-option
